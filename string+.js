console.log(2 + '4' + 5); // Output: '245'

// In this case, the + operator performs concatenation because one of the operands ('4') is a string. 
// JavaScript converts the other operands (2 and 5) to strings and concatenates them in order.
