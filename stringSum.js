console.log("10"+"1")//101


// 1.When "10" and "1" are enclosed in quotes,
//  they are treated as strings, not numbers.

// 2.Using the + operator between two strings ("10" and "1")
//  causes them to be concatenated (joined together),
//  resulting in the string "101".